VITE_HOSTNAME=http://localhost:3000

DB_PATH=./data/db.db

# Optional: 256-bit encryption key as hex string (64 characters)
# If not provided, a key will be auto-generated and stored in ./data/.encryption_key
# Generate a key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ENCRYPTION_KEY=